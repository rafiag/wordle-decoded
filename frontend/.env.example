# Frontend Environment Variables
# 
# IMPORTANT: For Docker development, set VITE_API_URL in the ROOT .env file
# (located at the project root, not in the frontend directory)
#
# The value is automatically passed from root .env via docker-compose.yml
#
# Default value: http://localhost:8000/api/v1
#
# For local development without Docker:
# Copy this file to .env and set your values
# VITE_API_URL=http://localhost:8000/api/v1
